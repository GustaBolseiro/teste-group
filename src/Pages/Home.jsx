import CounterFunction from "../Components/Counter";
import { Buttons } from "../Components/Buttons";

const Home = () => {
    return (
        <div>
            <CounterFunction />
            <Buttons/>
        </div>
    );
};

export default Home;